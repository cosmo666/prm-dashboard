# Frontend Conventions — see `angular-frontend.md`

This file is a redirect. Frontend conventions for PRM Dashboard live in **[`angular-frontend.md`](./angular-frontend.md)** — Angular 17 standalone components, NgRx Signal Store, ECharts wrappers, RBAC patterns.

This stub exists only so legacy references don't 404.
