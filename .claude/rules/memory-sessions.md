# Session Log

<!-- Claude: After completing substantive work, add a brief dated summary. Keep this file under 50 lines. Trim entries older than 2 weeks. -->

## Recent Sessions
- 2026-03-18: Audited and fixed .claude config — removed Pawgo content from 7 files, fixed agents.md references, updated hook, renamed gitignore, initialized git repo
- 2026-03-23: Updated CLAUDE.md with actual tech stack and directory structure. Connected CLAUDE.md to .claude infrastructure (agents, rules, hooks, skills). Updated stale rule files: removed node-backend.md, rewrote python-backend.md and react-frontend.md with actual conventions, updated architecture.md with stack details, synced memory-decisions.md.
- 2026-03-24: Implemented hour-level roster feature (14 phases, A-N). 12 new files, 19 modified. HourSlot model, fatigue scorer, skill gap detector, hour-slot assigner, 2 new validators, roster generator hourly mode, hourly coverage, auto-allocator update, 6 new API endpoints, slot CRUD with audit, Excel/PDF hourly exports, notification service, HourlyTimeline + FatigueDashboard frontend components. 163 tests (52 new). Updated README.md, CLAUDE.md, memory files.
- 2026-03-24: Fixed 5 critical/high/medium hourly roster bugs (B1-B5). B1: Phase 4a/4b dual-entry collision + full rule engine gating on hourly candidates. B2: OT assigner hourly mode (per-hour slots instead of fixed blocks). B3: Daily hours validators count shift+slot hours correctly for mixed employees. B4: Midnight-crossing slot timestamps shifted to correct calendar day. B5: Hourly entries include start/end times for rest gap validation. Also: anomaly checker covers all entry types, auto-allocator deduplicates pool, cancel buttons restyled red in frontend. 163 tests pass.
- 2026-03-24: Alembic/seeder sync audit — added missing roster_patterns table migration + seeder (10 patterns, 4 depts). Added SQLite DB files (*.db, *.db-shm, *.db-wal) to .gitignore. Fixed stale test count in README (111→163).
- 2026-03-24: Implemented Phase 6 Standby Assignment + Scenario Compare hourly support. New: standby_assigner.py, calculate_standby_priority (11 factors), calculate_deployed_coverage, 6 violation codes (E037-E038, W015-W017, I007). Rewired pipeline: Phase 5a→6→5b→8. Slot validator standby-aware. Anomaly checker: consecutive standby + task ratio checks. Auto-allocator: standby in pool + slot promotion with audit. Frontend: dual KPI, amber standby slots, roster card standby line. Compare: slot-level hourly diffs with mini-timeline bars. Fixed slot_start_dt early-morning false positive. Added rule engine validation to gap-fill per-skill. OT hourly candidate now uses full engine. 163 tests pass.
- 2026-03-25: API hardening + standby hardening. API: roster status guard on all slot mutations, slot_type enum validation, DELETE entry recalculation, audit entity_id backfill on create. Standby assigner: 9 fixes — per-hour engine validation (not day-level proxy), removed split_shift exclusion, holiday standby month filter, representative-hour fatigue scoring, peak-concurrent surplus calc, removed standby double-subtraction from regular hours, standby_off_pct guard, emp_lookup guard. 163 tests pass.
- 2026-03-25: Legacy gap closure — 4 features. (1) SystemConfig key-value store (model + service + CRUD API + 10 seeded defaults). (2) Multi-flight airline grouping: affinity scoring in auto-allocator + by-airline API endpoint. (3) Shift rounding buffers on ShiftRules. (4) ideal_time_between_service in SystemConfig. New files: system_config.py (model), system_config_service.py, system_config schemas, system_config API, seed_system_config.py. Migration c3f8e9a12b45. 162/163 tests pass.
- 2026-03-26: Roster Optimization with OR-Tools CP-SAT. New files: roster_optimizer.py (~300 lines), optimizer_constraints.py (~250 lines), optimizer_objectives.py (~180 lines). 18 hard constraints, 8 weighted soft objectives. Integrated into roster_generator.py with greedy fallback. API: optimizer + optimizer_time_limit params on /rosters/generate. Frontend: optimizer mode toggle, time limit input, solver stats, CP-SAT/Greedy badge on roster cards. Migration 31d1e216c938 (generation_method on rosters). 8 SystemConfig seed keys for optimizer weights. 27 new tests (190 total). Also: clarified employees are department-specific only (no cross-training), saved as project memory.
- 2026-03-27: Screen consolidation (overhaul plan steps 1–5). Merged 14 screens → 9. Rosters tab absorbs ScenarioCompare/FairnessDashboard/PatternEditor/RosterRun. Flights absorbs DemandRTD (Timeline tab). 5 page files deleted, 3 component files added, App.tsx routes updated, Sidebar regrouped. Dashboard hardcoded date fixed.
- 2026-03-29: Roster overhaul Phases 1+2. Phase 1: validator fixes (E039/E040 codes, restriction validator, break duration compliance, women_on_night_shift, holiday year, training_days, midnight coverage). Phase 2: constraint_registry.py + sequential_fallback.py created; deleted 4 greedy pipeline files; simplified roster_generator to 7-phase pipeline; fixed hour_slots.start_time NOT NULL (derive from hour at save time). 7 integration tests pass (103 unit + 7 integration = 110 passing).
- 2026-03-30: Documentation sync. system.md: rewritten for 7-phase pipeline, Sequential Fallback section added, sections renumbered (5-15), violation codes updated (38→40, 31→33 errors). README.md: test count 162→217, violation types 62→64. CLAUDE.md: test count 190→217, architecture decisions updated. 217 tests passing (216 unit + integration, 1 flaky integration test fixed).
- 2026-04-06: Previous month carryover for roster priority. Added _load_prev_month_carryover() to roster_generator.py (Phase 1b). Loads prior month roster into RosterState: last_shift_end, last_rest_day, prev_month_hours, prev_month_ot_hours, tail entries (last 7 days), tail hour_slots. Fixed consecutive_working_days_before() to include hourly_shift. Cleaned 40 orphaned April rosters from DB. Integration tests now auto-cleanup via _generate_and_track(). Updated stale employee count assertions (seed data grew). Generated March rosters for all 4 departments. 8 new tests (248 total). Updated CLAUDE.md, README.md, memory files.
