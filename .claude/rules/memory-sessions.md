# Session Log

<!-- Claude: After completing substantive work, add a brief dated summary. Keep this file under 50 lines. Trim entries older than 2 weeks. -->

## Recent Sessions

- 2026-04-09: **Polish + delight pass — items 14–20.** Login mouse parallax (grid 320ms toward cursor, cobalt glow 500ms inverted at 2× velocity, both `will-change: transform`). Per-tenant accent: `ThemeService` effect publishes `--tenant-accent` + `--tenant-accent-soft` (alpha 0.12) on root; 3px ribbon under top-bar brand mark. **Bug fix:** `tenant.resolver.ts` was reading snake_case (`logo_url`/`primary_color`) but the backend DTO is camelCase — tenant accent color was silently broken until now. Custom 404 (`features/not-found/`): editorial "Flight diverted" with `clamp(120px,20vw,220px)` serif, letter-spacing entrance, flight strip (origin from URL → UNKNOWN → NOT FOUND), random flight ID, two CTAs; replaced wildcard redirect in `app.routes.ts`. New `core/progress/progress.service.ts` (in-flight signal counter, start/stop/wrap) + `shared/components/progress-bar/` (2px fixed top, 1.4s cubic gradient sweep) mounted in AppComponent and wired into `authGuard` (silent refresh) + `tenant.resolver` (initial config). Route fade-in via CSS-only `::ng-deep router-outlet + *` selector (360ms cubic). New `shared/pipes/compact-number.pipe.ts` (15.2k / 1.5M, strips trailing zeros, null→"—") applied to Overview Total PRM and Fulfillment Requested + Total Provided KPIs. Filter feedback summary pill under page title with `summaryEnter` 400ms entrance + `summaryPulse` dot animation (1.6s ease-in-out, with `prefers-reduced-motion` opt-out).

- 2026-04-09: **Medium-value UX pass — items 8–13.** Date range picker: two-line trigger (label + resolved range in mono), per-preset inline ranges, ↑↓ keyboard nav with hint, Custom range row with inline `<input type="date">` panel (rendered as plain button outside `mat-menu-item` so menu doesn't auto-close). Airport selector: mono bold IATA + hairline divider + muted name beneath, plane icon, count badge, disabled when single airport. Dynamic breadcrumb: new `core/store/navigation.store.ts` + dashboard publishes active tab via effect, top-bar renders `Home / PRM Dashboard / <tab>` with `aria-current="page"` on the current segment, clears on dashboard `ngOnDestroy`. Custom `[appTooltip]` directive in `shared/directives/` (body-portal, viewport-clamped, 140ms fade, position+delay inputs, dark-mode aware) — replaces `matTooltip` everywhere; `matTooltip` is now an anti-pattern. Chart legends: all top, `selectedMode: 'multiple'`, `roundRect` 8×8 icons, donut center repositioned to `['50%', '58%']`, stone-300 inactive color in `chart-theme.ts`. Sankey: IBM Plex Mono labels, rich-text `{name|...} {val|...}` formatter, 8px nodes / 12px gap, 100px right padding for labels, gradient lines @ 0.2 opacity, 600ms cubicOut animation.

- 2026-04-09: **UI polish pass — closed 3 remaining high-value gaps.** (1) Replaced Material's default 2px tab bar with an inset pill: 12px horizontal inset, 1px height, `transform: scaleX()` with `transform-origin: left` + 320ms easing (styles.scss:320-337). (2) Top 10 agents `daysActive` column now renders editorially as `12d` with a cadence label beneath (`daily`/`frequent`/`regular`/`occasional`/`inactive`) — honest alternative to "last active" relative time since the API only returns active-day counts, not a `lastActiveAt` timestamp. (3) Prior audit confirmed empty states, skeleton loaders, dark mode, focus styles, chart card lift, KPI delta entrance animation, and agents table editorial treatment were already shipped.

- 2026-04-08: **All 9 phases complete — POC feature-complete.** Phases 5-9: Ocelot API Gateway (subdomain→X-Tenant-Slug, 3 routes), seed data (3 tenants, 12 employees, ~17k PRM records via Python generator), Angular 17 SPA (login, home, dashboard with 4 tabs: Overview/Top10/ServiceBreakup/Fulfillment), 6 ECharts wrappers, NgRx Signal Store filters, E2E checklist. 21 tasks total, subagent-driven with spec+quality reviews per phase.

- 2026-04-08: **Phases 2-4 complete.** Phase 2 (Auth Service): login/refresh/logout/me with BCrypt hashing, HS256 JWT, atomic refresh rotation, BCRYPT_PENDING bootstrap. Phase 3 (Tenant Service): tenant resolution, SchemaMigrator with embedded versioned migrations, 5-min connection cache, semaphore-guarded migration runner. Phase 4 (PRM Service): 23 endpoints across 7 controllers (KPIs, filters, trends, rankings, breakdowns, performance, records), airport RBAC middleware, TenantDbContextFactory for per-tenant DB access, PrmControllerBase, global exception handler. Review fixes: pinned MySqlServerVersion, fail-fast JWT config, atomic refresh CAS, negative duration guard, base controller extraction.

- 2026-04-08: **PRM Dashboard POC kickoff.** Wrote design spec (11 sections) and 21-task implementation plan (6,204 lines) covering .NET 8 microservices + Angular 17 + MySQL multi-tenant POC. Created GitHub repo `cosmo666/prm-dashboard`. Started subagent-driven execution.

- 2026-04-08: **Phase 1 complete.** Task 1: docker-compose + MySQL init scripts (master + 3 tenant DBs, prm_services table via stored procedure). Task 2: .NET 8 solution + PrmDashboard.Shared library (5 EF entities, 9 DTO files, TimeHelpers). Applied review fixes: authenticated MySQL healthcheck (prevents init-race), tenant env var cleanup, id column documentation, legacy .sln format for Dockerfile compatibility. All spec-compliant.

- 2026-04-08: **Multi-tenant design enhancement.** User raised runtime-tenant-onboarding requirement. Audited design and extended Task 5 plan with `SchemaMigrator` — embedded versioned SQL migrations (`backend/src/PrmDashboard.TenantService/Schema/Migrations/NNN_*.sql`) + `schema_migrations` tracker table per tenant. Result: attach a new MySQL DB on any host, INSERT one row in `prm_master.tenants`, schema auto-bootstraps on first request. No code changes, no restarts, no manual DDL. Schema evolution is fire-and-forget (never edit a committed migration; always add a new one).

- 2026-04-08: **Docs and .claude sync.** Created README.md + CLAUDE.md. Expanded .gitignore for full .NET + Angular + Docker tree. Replaced stale `rms-domain` skill with `prm-domain` (PRM domain knowledge: IATA SSR codes, HHMM time encoding, pause/resume dedup, query patterns). Deleted `python-backend.md` and `react-frontend.md`; created `dotnet-backend.md` and `angular-frontend.md`. Rewrote `architecture.md` for PRM. Appended PRM decisions to `memory-decisions.md`.
