services:
  # MySQL is only used by the legacy data-export pipeline
  # (tools/PrmDashboard.CsvExporter). Runtime services do NOT connect to it.
  # Gated behind the `data-pipeline` profile so `docker compose up` doesn't
  # start it; bring it up explicitly with:
  #   docker compose --profile data-pipeline up mysql -d
  mysql:
    image: mysql:8.0
    container_name: prm-mysql
    profiles: ["data-pipeline"]
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
    ports:
      - "3306:3306"
    volumes:
      - ./database/init:/docker-entrypoint-initdb.d
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root -p$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  gateway:
    build:
      context: ./backend
      dockerfile: src/PrmDashboard.Gateway/Dockerfile
    container_name: prm-gateway
    restart: unless-stopped
    ports:
      - "${GATEWAY_PORT:-5000}:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT:-Development}
      - AUTH_SERVICE_URL=http://auth:8080
      - TENANT_SERVICE_URL=http://tenant:8080
      - PRM_SERVICE_URL=http://prm:8080
      - Jwt__Secret=${JWT_SECRET:-your-256-bit-secret-key-change-in-production}
      - Jwt__Issuer=${JWT_ISSUER:-prm-dashboard}
      - Jwt__Audience=${JWT_AUDIENCE:-prm-dashboard-client}
      - Cors__AllowedOrigins__0=http://localhost:4200
      - Cors__AllowedOrigins__1=http://aeroground.localhost:4200
      - Cors__AllowedOrigins__2=http://skyserve.localhost:4200
      - Cors__AllowedOrigins__3=http://globalprm.localhost:4200
    depends_on:
      auth:
        condition: service_healthy
      tenant:
        condition: service_healthy
      prm:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  auth:
    build:
      context: ./backend
      dockerfile: src/PrmDashboard.AuthService/Dockerfile
    container_name: prm-auth
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT:-Development}
      - PRM_DATA_PATH=/app/data
      - Jwt__Secret=${JWT_SECRET:-your-256-bit-secret-key-change-in-production}
      - Jwt__Issuer=${JWT_ISSUER:-prm-dashboard}
      - Jwt__Audience=${JWT_AUDIENCE:-prm-dashboard-client}
      - Jwt__AccessTokenMinutes=${JWT_ACCESS_TOKEN_MINUTES:-15}
      - Jwt__RefreshTokenDays=${JWT_REFRESH_TOKEN_DAYS:-7}
      - Cors__AllowedOrigins__0=http://localhost:4200
      - Cors__AllowedOrigins__1=http://aeroground.localhost:4200
      - Cors__AllowedOrigins__2=http://skyserve.localhost:4200
      - Cors__AllowedOrigins__3=http://globalprm.localhost:4200
    volumes:
      - ./data:/app/data:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  tenant:
    build:
      context: ./backend
      dockerfile: src/PrmDashboard.TenantService/Dockerfile
    container_name: prm-tenant
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT:-Development}
      - PRM_DATA_PATH=/app/data
      - Jwt__Secret=${JWT_SECRET:-your-256-bit-secret-key-change-in-production}
      - Jwt__Issuer=${JWT_ISSUER:-prm-dashboard}
      - Jwt__Audience=${JWT_AUDIENCE:-prm-dashboard-client}
      - Cors__AllowedOrigins__0=http://localhost:4200
      - Cors__AllowedOrigins__1=http://aeroground.localhost:4200
      - Cors__AllowedOrigins__2=http://skyserve.localhost:4200
      - Cors__AllowedOrigins__3=http://globalprm.localhost:4200
    volumes:
      - ./data:/app/data:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  prm:
    build:
      context: ./backend
      dockerfile: src/PrmDashboard.PrmService/Dockerfile
    container_name: prm-prm
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT:-Development}
      - PRM_DATA_PATH=/app/data
      - Jwt__Secret=${JWT_SECRET:-your-256-bit-secret-key-change-in-production}
      - Jwt__Issuer=${JWT_ISSUER:-prm-dashboard}
      - Jwt__Audience=${JWT_AUDIENCE:-prm-dashboard-client}
      - Cors__AllowedOrigins__0=http://localhost:4200
      - Cors__AllowedOrigins__1=http://aeroground.localhost:4200
      - Cors__AllowedOrigins__2=http://skyserve.localhost:4200
      - Cors__AllowedOrigins__3=http://globalprm.localhost:4200
    volumes:
      - ./data:/app/data:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: prm-frontend
    restart: unless-stopped
    ports:
      - "4200:80"
    depends_on:
      gateway:
        condition: service_healthy

volumes:
  mysql-data:
